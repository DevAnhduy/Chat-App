const fs = require('fs');
const File_Model = require('src/models/')
class File_Query {
    constructor(path_file_folder,receiver_type,query_options){
        this.path_file_folder = path_file_folder,
        this.receiver_type = receiver_type,
        this.query_options = query_options
    }

    find(){
        
    }
}